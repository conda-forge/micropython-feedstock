{% set name = "micropython" %}
{% set version = "1.28.0" %}
{% set build = 0 %}
{% set sha256 = "1e77eb4450ce06098e6c8dba2dce1b2a4d885c24b74a2ba950dde3a1394d4ff6" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/micropython/micropython/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: {{ build }}
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - make
    - wget
    - unzip
  host:
    - libffi
    - pkgconfig

test:
  commands:
    - micropython -c "print('hello')"

about:
  home: https://micropython.org
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Python for microcontrollers
  description: |
    MicroPython is a lean and efficient implementation of the Python 3
    programming language that includes a small subset of the Python standard
    library and is optimised to run on microcontrollers and in constrained
    environments.
  dev_url: https://github.com/micropython/micropython

extra:
  recipe-maintainers:
    - davidbrochart
